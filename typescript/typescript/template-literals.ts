(function () {

    function getEdad() {
        return 100 + 1000 + 300
    }
    const nombre = 'Fernando'
    const apellidos = 'Perez Alcalde'
    const edad = 24

    const salida = `${nombre} ${apellidos} (${getEdad()})`

    console.log(salida)


})();

